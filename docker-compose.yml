services:
  app:
    build: .
    container_name: crypto_payment
    ports:
      - "80:3000"
    restart: always
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "5"    
